# Power BI Dashboard

Este directorio está destinado a almacenar el archivo de reporte `.pbix` de Power BI.

### Parámetros de Conexión:
* **Origen de datos:** Base de datos PostgreSQL
* **Servidor:** `localhost:5432`
* **Base de datos:** `triage_db`
* **Tabla principal:** `incidents` (renombrada a `incidentes` en Power Query)
* **Credenciales por defecto:** Usuario `admin`, Contraseña `password123`
